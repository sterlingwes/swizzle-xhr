{
  "name": "swizzle-xhr",
  "description": "Small lib for intercepting & transforming HTTP responses through XMLHttpRequest",
  "version": "1.2.0",
  "main": "swizzle-min.js",
  "license": "MIT",
  "scripts": {
    "document": "jsdoc2md swizzle.js > API.md",
    "build": "rollup -c",
    "test": "yarn build && jest"
  },
  "devDependencies": {
    "jest": "^25.3.0",
    "jsdoc-to-markdown": "^5.0.3",
    "rollup": "^2.6.1",
    "rollup-plugin-terser": "^5.3.0"
  }
}
